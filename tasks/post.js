var mm = require('marky-mark');

module.exports = function(grunt) {
  /*
   * disqus:
   *   title:
   *   slug:
   *   id:
   *   category:
   * page:
   *   title:
   *   description:
   *   keywords:
   */
  grunt.registerTask('post', 'Convert a markdown post to html', function() {
     
  });
};
